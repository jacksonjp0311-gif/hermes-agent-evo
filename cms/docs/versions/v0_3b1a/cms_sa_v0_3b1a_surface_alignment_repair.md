# CMS-SA v0.3b1a - Surface Alignment Repair and File-Run Guard

## Purpose

Repair the v0.3b1 alignment attempt by stabilizing section-level README currentness, mini README currentness, directory rows, and idempotent version-token checks.

## Boundary

This is not API implementation. It is a pre-API reflective feedback alignment repair.

## New Rule

Surface alignment must fail stale sections, but expected version tokens must not self-mutate.

## Non-claim lock

Surface alignment repair improves repository navigation and reflective feedback alignment only.