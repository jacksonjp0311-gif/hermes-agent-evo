COMMAND: python -c from pathlib import Path; import hrcn_runtime_bridge as b; assert b.assert_read_only_boundary(Path.cwd()) is True; print('live read-only boundary still passes')
RETURN CODE: 0
live read-only boundary still passes
