# RHP-011.2 README Geometry and Evidence Hygiene Closure

RHP-011.2 closes the repository geometry after RHP-011.1 made the gold-interface Rehydration Protocol visible.

## Closure target

```text
RHP-011.1 made runtime truth visible.
RHP-011.2 makes repository truth agree everywhere.
```

## Scope

- Root README current metrics.
- RHP activation charts.
- HRCN OPS bridge status.
- Post-seal cybernetic track.
- RHP failure-learning lock.
- RHP README local surface.
- Alignment guard latest evidence pointer.
- Evidence hygiene and scratch classification.

## Mandatory script telemetry contract

Every generated All-One, smoke, repair, and seal script must include:

```text
percent progress bar
dynamic BRANCH line
FUNC / COMMAND / OUTPUT / TIMEOUT lines
RHP_FAIL_LOCATION copy-back block
```

Large scripts must be run with:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\script.ps1
```

Boundary: documentation/evidence hygiene only. No provider call, model call, tool call, CMS write, memory promotion, API write, external ingestion, autonomy, or self-authorization.